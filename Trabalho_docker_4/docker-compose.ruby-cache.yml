services:
  api:
    image: linkextractor-api:ruby-cache
    build:
      context: ./api
      dockerfile: Dockerfile.ruby-cache
    ports:
      - "4567:4567"
    environment:
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./logs:/app/logs
  web:
    image: linkextractor-web:php
    build: ./www
    ports:
      - "80:80"
    environment:
      - API_ENDPOINT=http://api:4567/api/
    depends_on:
      - api
  redis:
    image: redis:7-alpine
  locust:
    image: locustio/locust:2.43.4
    ports:
      - "8089:8089"
    volumes:
      - ./:/mnt/locust:ro
    working_dir: /mnt/locust
    depends_on:
      - api
    command:
      - -f
      - /mnt/locust/loadtest/locustfile_ruby.py
      - --web-host
      - "0.0.0.0"
      - --web-port
      - "8089"
      - --host
      - http://api:4567
