services:
  api:
    image: linkextractor-api:python-cache
    build:
      context: ./api
      dockerfile: Dockerfile.python
    ports:
      - "5000:5000"
    environment:
      - REDIS_URL=redis://redis:6379
  web:
    image: linkextractor-web:php
    build: ./www
    ports:
      - "80:80"
    environment:
      - API_ENDPOINT=http://api:5000/api/
    depends_on:
      - api
  redis:
    image: redis:7-alpine
  locust:
    image: locustio/locust:2.43.4
    ports:
      - "8089:8089"
    volumes:
      - ./:/mnt/locust:ro
    working_dir: /mnt/locust
    depends_on:
      - api
    command:
      - -f
      - /mnt/locust/loadtest/locustfile.py
      - --web-host
      - "0.0.0.0"
      - --web-port
      - "8089"
      - --host
      - http://api:5000
